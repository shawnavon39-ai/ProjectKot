# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables (secrets)
.env
# NOTE: .env.production is NOT ignored — it only contains PUBLIC_ vars
# (same values already in wrangler.toml). Vite needs it at build time.

# discovery pipeline state
scripts/.discovered-urls.json
scripts/discovered-shops.json
scripts/scraped-shops.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.wrangler/
.vscode/
